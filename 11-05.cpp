map stores a pair of <key, value>, it is good to use map when we need retrive some info about a key
set sotres only keys, it is good to use map when have two state like bool. for example, presents of a key in  set is equivalent to true and if it is not there, it is equal to false.

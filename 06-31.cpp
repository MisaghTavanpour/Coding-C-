when we need a function to return an lvalue (function call is at the left side of an assigemnet), we can use function with a reference type to returns an lvalue.
when the return type. 
we cannot use a function with a const reference return type at the left side of an assignement.

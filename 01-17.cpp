/*
 If we keep giving same number as an input, nothing will be printed and code always will hit "if (val == currVal)" condition until we stop feeding the program and finally it stops and prints one line that actually shows how many time we repeated our input.
 */

/*
if (fval) --> convert float type to bool type

dval = fval + ival;  --> first convert ival type to float, then after add, convert the result to double.

dval + ival * cval;  --> cval type promote to int, then after multiplication, the reult of that will be promoted to double and adding is executed.
*/

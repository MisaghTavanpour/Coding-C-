using a container as a copy of another container: the container and element types must match.
using iterators: no need that the container types be same. the element types can differ as long as it is possible to convert the elements we’re copying to the element type of the container we are initializing.

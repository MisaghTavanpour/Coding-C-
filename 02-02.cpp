/*
    Based on their type (integer or real) and their max size and required precision, I would select the data type.
    For example, 'rate' usually is a small real number with few digits before and after the decimal point, therefore, float should be sufficient and efficient to represent it.
 */

/*
If we want the changes in the function change the value of the passing variable, then we need to pass it by reference (change lowercases to uppercase), otherwise we should pass by value (use variable as a counter in a loop condition).
*/
